metadata:
  namespace: demo
  name: zone-ingress
  labels:
    k8s.kuma.io/zone-proxy-type: ingress
spec:
  selector:
    app: zone-proxy
  ports:
    - name: zi-main
      protocol: TCP
      port: 80
      targetPort: 10001

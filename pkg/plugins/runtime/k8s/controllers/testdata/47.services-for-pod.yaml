metadata:
  namespace: demo
  name: example
spec:
  clusterIP: 192.168.0.1
  selector:
    app: example
    rollouts-pod-template-hash: blue
  ports:
    - protocol: TCP
      appProtocol: http
      port: 80
      targetPort: main

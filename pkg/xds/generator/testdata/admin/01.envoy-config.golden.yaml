resources:
  - name: kuma:envoy:admin
    resource:
      '@type': type.googleapis.com/envoy.api.v2.Cluster
      altStatName: kuma_envoy_admin
      connectTimeout: 5s
      loadAssignment:
        clusterName: kuma:envoy:admin
        endpoints:
          - lbEndpoints:
              - endpoint:
                  address:
                    socketAddress:
                      address: 127.0.0.1
                      portValue: 9901
      name: kuma:envoy:admin
      type: STATIC
  - name: kuma:envoy:admin
    resource:
      '@type': type.googleapis.com/envoy.api.v2.Listener
      address:
        socketAddress:
          address: 192.168.0.1
          portValue: 9901
      filterChains:
        - filters:
            - name: envoy.filters.network.http_connection_manager
              typedConfig:
                '@type': type.googleapis.com/envoy.config.filter.network.http_connection_manager.v2.HttpConnectionManager
                httpFilters:
                  - name: envoy.filters.http.router
                routeConfig:
                  validateClusters: false
                  virtualHosts:
                    - domains:
                        - '*'
                      name: envoy_admin
                      routes:
                        - match:
                            prefix: /clusters
                          route:
                            cluster: kuma:envoy:admin
                            prefixRewrite: /clusters
                        - match:
                            prefix: /config_dump
                          route:
                            cluster: kuma:envoy:admin
                            prefixRewrite: /config_dump
                        - match:
                            prefix: /listeners
                          route:
                            cluster: kuma:envoy:admin
                            prefixRewrite: /listeners
                        - match:
                            prefix: /listeners
                          route:
                            cluster: kuma:envoy:admin
                            prefixRewrite: /listeners
                        - match:
                            prefix: /quitquitquit
                          route:
                            cluster: kuma:envoy:admin
                            prefixRewrite: /quitquitquit
                        - match:
                            prefix: /ready
                          route:
                            cluster: kuma:envoy:admin
                            prefixRewrite: /ready
                        - match:
                            prefix: /stats
                          route:
                            cluster: kuma:envoy:admin
                            prefixRewrite: /stats
                statPrefix: kuma_envoy_admin
      name: kuma:envoy:admin
      trafficDirection: INBOUND

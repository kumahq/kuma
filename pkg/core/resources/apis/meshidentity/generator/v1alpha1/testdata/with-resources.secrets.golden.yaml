resources:
- name: test-secret
  resource:
    '@type': type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.Secret
    name: test-secret
    validationContext:
      trustedCa:
        environmentVariable: MY_ENV

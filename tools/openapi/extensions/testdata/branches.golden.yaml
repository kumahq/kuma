- properties:
    config:
      $ref: '#/components/schemas/FakeResourceAcmpcaExtensionConfig'
    type:
      const: acmpca
  title: acmpca
- properties:
    config:
      $ref: '#/components/schemas/FakeResourceVaultExtensionConfig'
    type:
      const: vault
  title: vault
- description: An extension this control plane does not ship. Its configuration is
    not described here.
  properties:
    type:
      not:
        enum:
        - acmpca
        - vault
  title: Other

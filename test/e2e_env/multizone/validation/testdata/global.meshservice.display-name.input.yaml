type: MeshService
name: ms-display-name
mesh: multizone-label-validation
labels:
  kuma.io/display-name: my-custom-name
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

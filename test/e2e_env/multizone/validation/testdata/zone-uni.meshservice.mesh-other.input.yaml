type: MeshService
name: ms-mesh-other
mesh: multizone-label-validation
labels:
  kuma.io/mesh: nonexistent-mesh
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

apiVersion: kuma.io/v1alpha1
kind: MeshExternalService
metadata:
  name: mes-mesh-other
  namespace: {{ Config.KumaNamespace }}
  labels:
    kuma.io/mesh: nonexistent-mesh
    kuma.io/origin: zone
spec:
  match:
    type: HostnameGenerator
    port: 80
    protocol: http

type: MeshExternalService
name: mes-proxy-type
mesh: multizone-label-validation
labels:
  kuma.io/origin: zone
  kuma.io/proxy-type: sidecar
spec:
  match:
    type: HostnameGenerator
    port: 80
    protocol: http

apiVersion: kuma.io/v1alpha1
kind: MeshService
metadata:
  name: ms-mesh-other
  namespace: default
  labels:
    kuma.io/mesh: nonexistent-mesh
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

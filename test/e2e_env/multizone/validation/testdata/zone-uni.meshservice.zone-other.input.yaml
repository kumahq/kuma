type: MeshService
name: ms-zone-other
mesh: multizone-label-validation
labels:
  kuma.io/zone: other-zone
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

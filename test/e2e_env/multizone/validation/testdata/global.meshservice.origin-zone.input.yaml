type: MeshService
name: ms-origin-zone
mesh: multizone-label-validation
labels:
  kuma.io/origin: zone
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

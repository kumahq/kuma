type: MeshExternalService
name: mes-arbitrary-user
mesh: multizone-label-validation
labels:
  kuma.io/foo: bar
  k8s.kuma.io/baz: qux
spec:
  match:
    type: HostnameGenerator
    port: 80
    protocol: http

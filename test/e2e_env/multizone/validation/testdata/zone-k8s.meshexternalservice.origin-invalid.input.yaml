apiVersion: kuma.io/v1alpha1
kind: MeshExternalService
metadata:
  name: mes-origin-invalid
  namespace: {{ Config.KumaNamespace }}
  labels:
    kuma.io/mesh: multizone-label-validation
    kuma.io/origin: bogus
spec:
  match:
    type: HostnameGenerator
    port: 80
    protocol: http

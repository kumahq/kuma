apiVersion: kuma.io/v1alpha1
kind: MeshExternalService
metadata:
  name: mes-policy-role
  namespace: kuma-system
  labels:
    kuma.io/mesh: multizone-label-validation
    kuma.io/origin: zone
    kuma.io/policy-role: system
spec:
  match:
    type: HostnameGenerator
    port: 80
    protocol: http

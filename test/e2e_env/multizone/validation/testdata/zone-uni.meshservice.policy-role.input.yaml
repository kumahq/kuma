type: MeshService
name: ms-policy-role
mesh: multizone-label-validation
labels:
  kuma.io/policy-role: system
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

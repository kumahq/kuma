type: MeshService
name: ms-env
mesh: multizone-label-validation
labels:
  kuma.io/env: universal
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

apiVersion: kuma.io/v1alpha1
kind: MeshService
metadata:
  name: ms-origin-global
  namespace: default
  labels:
    kuma.io/mesh: multizone-label-validation
    kuma.io/origin: global
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

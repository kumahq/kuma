apiVersion: kuma.io/v1alpha1
kind: MeshService
metadata:
  name: ms-kube-namespace
  namespace: default
  labels:
    kuma.io/mesh: multizone-label-validation
    k8s.kuma.io/namespace: kuma-system
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

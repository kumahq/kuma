type: MeshService
name: ms-workload
mesh: multizone-label-validation
labels:
  kuma.io/workload: my-workload
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

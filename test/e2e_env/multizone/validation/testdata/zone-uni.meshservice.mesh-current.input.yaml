type: MeshService
name: ms-mesh-current
mesh: multizone-label-validation
labels:
  kuma.io/mesh: multizone-label-validation
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

type: MeshService
name: ms-origin-invalid
mesh: multizone-label-validation
labels:
  kuma.io/origin: bogus
spec:
  ports:
    - port: 80
      appProtocol: http
  selector:
    dataplaneTags:
      kuma.io/service: my-service

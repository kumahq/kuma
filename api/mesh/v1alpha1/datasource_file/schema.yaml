components:
  schemas:
    DataSource_File:
      properties:
        file:
          description: |-
            Data source is a path to a file.
            Deprecated, use other sources of a data.
          type: string
      type: object
info:
  x-ref-schema-name: DataSource_File
openapi: 3.1.0

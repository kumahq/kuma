components:
  schemas:
    DatadogTracingBackendConfig:
      properties:
        address:
          type: string
        port:
          type: integer
        splitService:
          type: boolean
      type: object
info:
  x-ref-schema-name: DatadogTracingBackendConfig
openapi: 3.1.0

components:
  schemas:
    DataSource_Inline:
      properties:
        inline:
          format: byte
          type: string
      type: object
info:
  x-ref-schema-name: DataSource_Inline
openapi: 3.1.0

components:
  schemas:
    ZipkinTracingBackendConfig:
      properties:
        apiVersion:
          type: string
        sharedSpanContext:
          type: boolean
        traceId128bit:
          type: boolean
        url:
          type: string
      type: object
info:
  x-ref-schema-name: ZipkinTracingBackendConfig
openapi: 3.1.0

components:
  schemas:
    DataSource_Secret:
      properties:
        Secret:
          description: Data source is a secret with given Secret key.
          type: string
        type:
          const: secret
          type: string
      type: object
info:
  x-ref-schema-name: DataSource_Secret
openapi: 3.1.0

type: MeshExternalService
name: mes-mesh-current
mesh: multizone-label-validation
labels:
  kuma.io/origin: zone
  kuma.io/mesh: multizone-label-validation
spec:
  match:
    type: HostnameGenerator
    port: 80
    protocol: http

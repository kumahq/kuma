---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: meshopentelemetrybackends.kuma.io
spec:
  group: kuma.io
  names:
    categories:
    - kuma
    kind: MeshOpenTelemetryBackend
    listKind: MeshOpenTelemetryBackendList
    plural: meshopentelemetrybackends
    shortNames:
    - motb
    singular: meshopentelemetrybackend
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MeshOpenTelemetryBackend defines a shared OTel collector endpoint
          for observability policies.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec is the specification of the Kuma MeshOpenTelemetryBackend
              resource.
            properties:
              endpoint:
                description: Endpoint defines the OTel collector address and port.
                properties:
                  address:
                    description: Address of the OTel collector (hostname or IP).
                    minLength: 1
                    type: string
                  path:
                    description: |-
                      Path is an optional base path prefix for HTTP endpoints.
                      The CP appends signal-specific suffixes (/v1/traces, /v1/metrics, /v1/logs).
                      Ignored for gRPC.
                    type: string
                  port:
                    description: Port of the OTel collector.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - address
                - port
                type: object
              protocol:
                allOf:
                - enum:
                  - grpc
                  - http
                - enum:
                  - grpc
                  - http
                default: grpc
                description: Protocol selects gRPC or HTTP transport for the collector
                  connection.
                type: string
            required:
            - endpoint
            type: object
        type: object
    served: true
    storage: true

name: sample
mesh: default
type: Dataplane
networking:
  address: "{{ address }}"
  inbound:
    - port: 80
---
name: sample-2
mesh: default
type: Dataplane
networking:
  address: "{{ address }}"
  inbound:
    - port: 80
---
name: sample-3
mesh: default
type: Dataplane
networking:
  address: "{{ address }}"
  inbound:
    - port: 80

controlPlane:
  mode: zone
  zone: east
  kdsGlobalAddress: "grpcs://foo.com:3456"
  tls:
    general:
      secretName: my-certs
      caSecretName: my-custom-ca-name
ingress:
  enabled: true
egress:
  enabled: true

name: project-lifecycle
on:
  schedule:
    - cron: 0 7 * * *
  workflow_dispatch:
  issues:
    types:
      - reopened
      - opened
      - labeled
permissions:
  contents: read
jobs:
  lifecycle:
    permissions:
      issues: write
    uses: kumahq/.github/.github/workflows/wfc_lifecycle.yml@d266a5173018b554838bc167fedd4a2bd08add64
    with:
      filesToIgnore: CONTRIBUTING.md
    secrets:
      app_id: ${{ secrets.APP_ID }}
      private_key: ${{ secrets.APP_PRIVATE_KEY }}

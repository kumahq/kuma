name: "Notify about updates to Kuma (updated)"
on:
  push:
    branches:
      - lobkovilya-test-workflow
permissions:
  contents: read
jobs:
  notify-about-merged-pr:
    timeout-minutes: 10
    name: "Notify about merged PR (2)"
    runs-on: ubuntu-24.04
    steps:
      - name: Hello World
        run: |
          echo "Hello World"

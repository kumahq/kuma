contexts:
- controlPlane: example
  name: example
controlPlanes:
- coordinates:
    kubernetes:
      context: minikube
      kubeconfig: /home/user/.kube/config
      namespace: konvoy-system
  name: example
currentContext: example

---
apiVersion: v1
kind: Namespace
metadata:
  name: demo2-meshes-mobile
  labels:
    getkonvoy.io/sidecar-injection: enabled

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mobile-config
  namespace: demo2-meshes-mobile
data:
  kong.yml: |
    _format_version: "1.1"

    services:
    - name: mockbin.org
      url: http://mockbin.org
      routes:
      - name: default
        paths:
        - /
---
apiVersion: v1
kind: Service
metadata:
  name: mobile
  namespace: demo2-meshes-mobile
spec:
  ports:
  - port: 80
    name: http
    targetPort: 8000
  selector:
    app: mobile
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mobile-service-account
  namespace: demo2-meshes-mobile
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile
  namespace: demo2-meshes-mobile
  labels:
    app: mobile
spec:
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: mobile
  template:
    metadata:
      labels:
        app: mobile
      annotations:
        getkonvoy.io/mesh: pilot
    spec:
      serviceAccountName: mobile-service-account
      containers:
      - name: mobile
        image: "kong:1.1.2-alpine"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        env:
        - name: KONG_ADMIN_LISTEN
          value: "off"
        - name: KONG_DATABASE
          value: "off"
        - name: KONG_DECLARATIVE_CONFIG
          value: "/etc/kong/kong.yml"
        volumeMounts:
        - name: mobile-config
          mountPath: /etc/kong
        livenessProbe:
          exec:
            command:
            - curl
            - -s
            - http://localhost:8000
        readinessProbe:
          exec:
            command:
            - curl
            - -s
            - http://localhost:8000
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
      volumes:
      - name: mobile-config
        configMap:
          name: mobile-config
